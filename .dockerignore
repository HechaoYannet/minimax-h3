# Build context for docker/Dockerfile.  Kept small on purpose: the weights, the
# generated media and the caches are all runtime mounts, never build inputs.
.git
.gitignore
.gitattributes
models
workspace
cache/text_embed
cache/sweep
docker/out
**/__pycache__
*.pyc
*.tmp
